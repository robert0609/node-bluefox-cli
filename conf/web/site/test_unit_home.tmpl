import { expect } from 'chai';
import home from '../../src/pages/home';

describe('home_test', function () {
	before(function () {
		// 在本区块的所有测试用例之前执行
	});

	after(function () {
		// 在本区块的所有测试用例之后执行
	});

	beforeEach(function () {
		// 在本区块的每个测试用例之前执行
	});

	afterEach(function () {
		// 在本区块的每个测试用例之后执行
	});

	it('should_be_rendered_correctly', () => {
		let ele = home.render();
		let text = ele.querySelector('h2').innerText;
		expect(text).to.be.equal('Hello world!');
	});
});
